<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        h1 { color: #333; }
        p { font-size: 18px; }
        .logout-btn { display: inline-block; margin-top: 20px; padding: 10px 20px; background-color: #dc3545; color: white; text-decoration: none; border-radius: 5px; }
        .logout-btn:hover { background-color: #c82333; }
    </style>
<%- include('header.ejs') %>
</head>
<body>
    <div class="page">
<%- include('body.ejs') %>
        <div class="body">
            <div class="container">
                <h1><%= title %></h1>
                <p>Welcome, <%= user %>!</p>
                <p>This is a protected area only accessible after login.</p>
                
                <div>
                    <a href="/">Home</a>
                    <a href="/fuzzers">Manage Fuzzers</a>
                </div>

                <a href="/logout" class="logout-btn">Logout</a>
            </div>
        </div>
    </div>
</body>
</html>
