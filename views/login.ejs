<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        form { display: inline-block; text-align: left; padding: 20px; border-radius: 5px; }
        label { display: block; margin-bottom: 5px; margin-top: 10px; }
        input { width: 100%; padding: 8px; box-sizing: border-box; }
        button { width: 100%; margin: 10px 0 10px 0; background-color: #007bff; color: white; border: none; cursor: pointer; }
        button:hover { background-color: #0056b3; }
        .error { color: red; margin-bottom: 15px; }
    </style>
<%- include('header.ejs') %>
</head>
<body>
<%- include('body.ejs') %>
                    <h1><%= title %></h1>
                    <div style="text-align: center;">
                        <form action="/login?cb=<%= encodeURIComponent(cb) %>" method="POST">
                            <% if (error) { %>
                                <div class="error"><%= error %></div>
                            <% } %>
                            <div>
                                <label for="username">Username:</label>
                                <input type="text" id="username" name="username" required>
                            </div>
                            <div>
                                <label for="password">Password:</label>
                                <input type="password" id="password" name="password" required>
                            </div>
                            <button type="submit">Login</button>
                        </form>
                    </div>
<%- include('body-end.ejs') %>
<%- include('footer.ejs') %>
</body>
</html>
